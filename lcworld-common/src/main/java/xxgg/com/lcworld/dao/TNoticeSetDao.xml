<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lcworld.dao.TNoticeSetDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.lcworld.entity.TNoticeSetEntity" id="tNoticeSetMap">
        <result property="id" column="id"/>
        <result property="uid" column="uid"/>
        <result property="noticetype" column="noticetype"/>
        <result property="status" column="status"/>
    </resultMap>

	<select id="queryObject" resultType="com.lcworld.entity.TNoticeSetEntity">
		select * from t_notice_set where id = #{value}
	</select>

	<select id="queryList" resultType="com.lcworld.entity.TNoticeSetEntity">
		select * from t_notice_set
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from t_notice_set 
	</select>
	 
	<insert id="save" parameterType="com.lcworld.entity.TNoticeSetEntity" useGeneratedKeys="true" keyProperty="id">
		insert into t_notice_set
		(
			`uid`, 
			`noticetype`, 
			`status`
		)
		values
		(
			#{uid}, 
			#{noticetype}, 
			#{status}
		)
	</insert>
	 
	<update id="update" parameterType="com.lcworld.entity.TNoticeSetEntity">
		update t_notice_set 
		<set>
			<if test="uid != null">`uid` = #{uid}, </if>
			<if test="noticetype != null">`noticetype` = #{noticetype}, </if>
			<if test="status != null">`status` = #{status}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from t_notice_set where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from t_notice_set where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>