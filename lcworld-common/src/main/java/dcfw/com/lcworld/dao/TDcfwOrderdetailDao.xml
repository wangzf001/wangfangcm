<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lcworld.dao.TDcfwOrderdetailDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.lcworld.entity.TDcfwOrderdetailEntity" id="tDcfwOrderdetailMap">
        <result property="id" column="id"/>
        <result property="orderid" column="orderid"/>
        <result property="foodid" column="foodid"/>
        <result property="count" column="count"/>
        <result property="price" column="price"/>
        <result property="createtime" column="createtime"/>
        <result property="totalprice" column="totalprice"/>
    </resultMap>

	<select id="queryObject" resultType="com.lcworld.entity.TDcfwOrderdetailEntity">
		select * from t_dcfw_orderdetail where id = #{value}
	</select>

	<select id="queryList" resultType="com.lcworld.entity.TDcfwOrderdetailEntity">
		SELECT
			od.*, 
			f.`name` foodName,
			f.img foodImg,
			f.original foodOriginal,
			f.remain foodRemain
		FROM
			t_dcfw_orderdetail od
		LEFT JOIN t_dcfw_food f ON f.id = od.foodid
		<where>
			<if test="idArr!=null">
				od.id in
				<foreach item="id" collection="idArr" open="(" separator="," close=")">
					#{id}
				</foreach>
			</if>
			<if test="oid!=null">od.orderid = #{oid}</if>
		</where>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from t_dcfw_orderdetail od
		<where>
			<if test="oid!=null">od.orderid = #{oid}</if>
		</where>
	</select>
	 
	<insert id="save" parameterType="com.lcworld.entity.TDcfwOrderdetailEntity" useGeneratedKeys="true" keyProperty="id">
		insert into t_dcfw_orderdetail
		(
			`orderid`, 
			`foodid`, 
			`count`, 
			`price`, 
			`createtime`, 
			`totalprice`
		)
		values
		(
			#{orderid}, 
			#{foodid}, 
			#{count}, 
			#{price}, 
			#{createtime}, 
			#{totalprice}
		)
	</insert>
	 
	<update id="update" parameterType="com.lcworld.entity.TDcfwOrderdetailEntity">
		update t_dcfw_orderdetail 
		<set>
			<if test="orderid != null">`orderid` = #{orderid}, </if>
			<if test="foodid != null">`foodid` = #{foodid}, </if>
			<if test="count != null">`count` = #{count}, </if>
			<if test="price != null">`price` = #{price}, </if>
			<if test="createtime != null">`createtime` = #{createtime}, </if>
			<if test="totalprice != null">`totalprice` = #{totalprice}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from t_dcfw_orderdetail where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from t_dcfw_orderdetail where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>