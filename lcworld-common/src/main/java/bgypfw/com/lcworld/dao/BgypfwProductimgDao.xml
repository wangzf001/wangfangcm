<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lcworld.dao.BgypfwProductimgDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.lcworld.entity.BgypfwProductimgEntity" id="bgypfwProductimgMap">
        <result property="id" column="id"/>
        <result property="productId" column="product_id"/>
        <result property="img" column="img"/>
        <result property="sort" column="sort"/>
    </resultMap>

	<select id="queryObject" resultType="com.lcworld.entity.BgypfwProductimgEntity">
		SELECT
			p.*, GROUP_CONCAT(i.img ORDER BY i.sort) imgs
		FROM
			t_bgypfw_product p
		LEFT JOIN t_bgypfw_productimg i ON i.product_id = p.id
		WHERE
			p.id = #{value}
	</select>

	<select id="queryList" resultType="com.lcworld.entity.BgypfwProductimgEntity">
		select * from t_bgypfw_productimg
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from t_bgypfw_productimg 
	</select>
	 
	<insert id="save" parameterType="com.lcworld.entity.BgypfwProductimgEntity" useGeneratedKeys="true" keyProperty="id">
		insert into t_bgypfw_productimg
		(
			`product_id`, 
			`img`, 
			`sort`
		)
		values
		(
			#{productId}, 
			#{img}, 
			#{sort}
		)
	</insert>
	<insert id="saveBatch" parameterType="java.util.List">
		insert into t_bgypfw_productimg
		(
			`product_id`, 
			`img`, 
			`sort`
		)
		values
		<foreach item="img" collection="list" separator="," >
		(
			#{img.productId}, 
			#{img.img}, 
			#{img.sort}
		)
		</foreach>
	</insert>
	 
	<update id="update" parameterType="com.lcworld.entity.BgypfwProductimgEntity">
		update t_bgypfw_productimg 
		<set>
			<if test="productId != null">`product_id` = #{productId}, </if>
			<if test="img != null">`img` = #{img}, </if>
			<if test="sort != null">`sort` = #{sort}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from t_bgypfw_productimg where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from t_bgypfw_productimg where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>
	<delete id="deleteByPids">
		delete from t_bgypfw_productimg where product_id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>