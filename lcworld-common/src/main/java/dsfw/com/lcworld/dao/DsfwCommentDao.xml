<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lcworld.dao.DsfwCommentDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.lcworld.entity.DsfwCommentEntity" id="dsfwCommentMap">
        <result property="id" column="id"/>
        <result property="imgs" column="imgs"/>
        <result property="content" column="content"/>
        <result property="orderid" column="orderid"/>
        <result property="uid" column="uid"/>
        <result property="createtime" column="createtime"/>
        <result property="score" column="score"/>
        <result property="anonymous" column="anonymous"/>
        <result property="servicescore" column="servicescore"/>
        <result property="productscore" column="productscore"/>
        <result property="type" column="type"/>
        <result property="menderid" column="menderid"/>
        <result property="replyname" column="replyname"/>
        <result property="replyphoto" column="replyphoto"/>
    </resultMap>

	<select id="queryObject" resultType="com.lcworld.entity.DsfwCommentEntity">
		select * from t_dsfw_comment where id = #{value}
	</select>

	<select id="queryList" resultType="com.lcworld.entity.DsfwCommentEntity">
	select c.id,c.orderid,c.uid,c.content,c.servicescore,c.score,c.productscore,c.createtime,c.imgs,c.anonymous,
		<if test="auth!=null and auth == 0"> 
		CASE c.anonymous WHEN 0 THEN IFNULL(u.realname,u.user_name)
		WHEN 1  THEN "无权查看"
		END  user_name, 
		CASE c.anonymous WHEN 0 THEN u.mobile 
		WHEN 1  THEN "无权查看" 
		END  mobile,
		CASE c.anonymous WHEN 0 THEN u.photo 
		WHEN 1  THEN "无权查看" 
		END  photo
		</if>
		<if test="auth!=null and auth == 1"> 
		IFNULL(u.realname,u.user_name) user_name, u.mobile, u.photo 
		</if> 
		from t_dsfw_comment c
		left join t_user u on u.id = c.uid
		<where>
			<if test="ids!=null">c.id in 
				<foreach item="id" collection="ids" open="(" separator="," close=")">
					#{id}
				</foreach>
			</if>
			<if test="username != null and username.trim()!=''">LOCATE(#{username}, CONCAT(u.user_name,u.realname)) > 0</if>
		</where>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from t_dsfw_comment c
		left join t_user u on u.id = c.uid
		<where>
			<if test="username != null and username.trim()!=''">u.user_name like CONCAT("%",#{username},"%")</if>
		</where>
	</select>
	
	<select id="queryCommentTotal" resultType="int">
       	select  count(*)   from t_dsfw_comment  t 
		LEFT  JOIN  (select * from t_delivery_order o where o.ordertype=18) d 
		on t.orderid = d.orderid
		  LEFT JOIN  t_user u
		  ON t.uid = u.id
		  where 1=1
        <if test="uid != null">
            and d.did = #{uid}
        </if>
        <if test="scoretype == 1 ">
            and t.score &lt;2
        </if>
         <if test="scoretype == 2 ">
            and t.score between 2 and 4
        </if>
         <if test="scoretype == 3 ">
            and t.score &gt;=4
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by t.id desc
            </otherwise>
        </choose>
    </select>
	 
	 
	 
	<insert id="save" parameterType="com.lcworld.entity.DsfwCommentEntity" useGeneratedKeys="true" keyProperty="id">
		insert into t_dsfw_comment
		(
			`imgs`, 
			`content`, 
			`orderid`, 
			`uid`, 
			`createtime`, 
			`score`, 
			`anonymous`,
			`servicescore`,
			`productscore`,
			`type`
		)
		values
		(
			#{imgs}, 
			#{content}, 
			#{orderid}, 
			#{uid}, 
			#{createtime}, 
			#{score}, 
			#{anonymous},
			#{servicescore},
			#{productscore},
			#{type}
		)
	</insert>
	<insert id="addImgList" parameterType="java.util.Map">
		insert into t_dsfw_comment_img
		(
			img_path,
			createtime,
			comment_id
		) 
		VALUES 
		<foreach item="img" collection="imgList" separator=",">
		(
			#{img.imgUrl},
			#{img.createTime},
			#{img.commentId}
		)
		</foreach>
	</insert>
	<update id="update" parameterType="com.lcworld.entity.DsfwCommentEntity">
		update t_dsfw_comment 
		<set>
			<if test="imgs != null">`imgs` = #{imgs}, </if>
			<if test="content != null">`content` = #{content}, </if>
			<if test="orderid != null">`orderid` = #{orderid}, </if>
			<if test="uid != null">`uid` = #{uid}, </if>
			<if test="createtime != null">`createtime` = #{createtime}, </if>
			<if test="score != null">`score` = #{score}, </if>
			<if test="anonymous != null">`anonymous` = #{anonymous},</if>
			<if test="servicescore != null">`servicescore` = #{servicescore},</if>
			<if test="productscore != null">`productscore` = #{productscore}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from t_dsfw_comment where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from t_dsfw_comment where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>
	
	    
     <select id="queryListByScoretype" resultMap="dsfwCommentMap">
       	select t.*,u.realname  as 'replyname',u.photo as 'replyphoto',d.did  from t_dsfw_comment  t 
		LEFT  JOIN  (select * from t_delivery_order o where o.ordertype=18) d 
		on t.orderid = d.orderid
		  LEFT JOIN  t_user u
		  ON t.uid = u.id
			where 1=1
        <if test="uid != null">
            and d.did = #{uid}
        </if>
        <if test="scoretype == 1 ">
            and t.score &lt;2
        </if>
         <if test="scoretype == 2 ">
            and t.score between 2 and 4
        </if>
         <if test="scoretype == 3 ">
            and t.score &gt;=4
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by t.id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>
	
</mapper>