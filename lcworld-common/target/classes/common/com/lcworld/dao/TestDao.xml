<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lcworld.dao.TestDao">
	<resultMap type="com.lcworld.entity.TestEntity" id="testMap">
        <result property="menuId" column="menu_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <collection property="list" javaType="com.lcworld.entity.TestEntity">
	        <result property="menuId" column="sid"/>
	        <result property="name" column="sname"/>
	        <result property="parentId" column="sparentid"/>
        </collection>
    </resultMap>
	<select id="queryObject" resultType="com.lcworld.entity.TestEntity">
		select * from sys_menu where menu_id = #{value}
	</select>
	
	<insert id="save" parameterType="com.lcworld.entity.TestEntity">
		insert into sys_menu
		(
			`menu_id`, 
			`parent_id`, 
			`name`, 
			`url`, 
			`perms`, 
			`type`, 
			`icon`, 
			`order_num`
		)
		values
		(
			#{menuId}, 
			#{parentId}, 
			#{name}, 
			#{url}, 
			#{perms}, 
			#{type}, 
			#{icon}, 
			#{orderNum}
		)
	</insert>
	
	<select id="queryListByUser" resultMap="testMap">
		SELECT
			m.*,sm.parent_id sparentid,sm.menu_id sid,sm.name sname
		FROM
			sys_menu m
		LEFT JOIN sys_menu sm ON sm.parent_id = m.menu_id
		WHERE
			m.type = 0
		AND FIND_IN_SET(
			sm.menu_id,
			(
				SELECT
					GROUP_CONCAT(CAST(m2.menu_id AS CHAR(10))) aa
				FROM
					sys_menu m2
				WHERE
					m2.parent_id = m.menu_id
			)
		)
	</select>
	<select id="queryListParentId" resultType="com.lcworld.entity.TestEntity">
		select * from sys_menu where parent_id = #{parentId} order by order_num asc 
	</select>
	
	<select id="queryNotButtonList" resultType="com.lcworld.entity.TestEntity">
		select * from sys_menu where type != 2 order by order_num asc 
	</select>
	
	<select id="queryList" resultType="com.lcworld.entity.TestEntity">
		select m.*,(select p.name from sys_menu p where p.menu_id = m.parent_id) as parentName 
			from sys_menu m
		<where>
			<if test="keyword!=null and keyword.trim()!=''">
				<if test="chars!=null">
					<!-- <foreach item="c" collection="chars" separator="or" >
						m.name LIKE CONCAT("%",#{c},"%")
					</foreach> -->
					m.name LIKE CONCAT("%",#{keyword},"%")
				</if>
				<if test="pid!=null">
					m.parent_id = #{pid}
				</if>
			</if>
		</where>
		<choose>
			<when test="sidx != null and sidx.trim() != ''">
				order by m.${sidx} ${order}
			</when>
			<otherwise>
				order by m.order_num asc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
	<!-- 查询用户的权限列表 --> 
	<select id="queryUserList" resultType="com.lcworld.entity.TestEntity">
		select distinct m.*,(select p.name from sys_menu p where p.menu_id = m.parent_id) as parentName
			from sys_user_role ur 
			LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id 
			LEFT JOIN sys_menu m on rm.menu_id = m.menu_id 
		where ur.user_id = #{userId} order by m.order_num asc
	</select>
	<select id="queryTotal" resultType="int">
		select count(*) from sys_menu m
		<where>
			<if test="keyword!=null and keyword.trim()!=''">
				<if test="chars!=null">
					<!-- <foreach item="c" collection="chars" separator="or" >
						m.name LIKE CONCAT("%",#{keyword},"%")
					</foreach> -->
					m.name LIKE CONCAT("%",#{keyword},"%")
				</if>
				<if test="pid!=null">
					m.parent_id = #{pid}
				</if>
			</if>
		</where>
	</select>
	 
	<update id="update" parameterType="com.lcworld.entity.TestEntity">
		update sys_menu 
		<set> 
			<if test="parentId != null">`parent_id` = #{parentId}, </if>
			<if test="name != null">`name` = #{name}, </if>
			<if test="url != null">`url` = #{url}, </if>
			<if test="perms != null">`perms` = #{perms}, </if>
			<if test="type != null">`type` = #{type}, </if>
			<if test="icon != null">`icon` = #{icon}, </if>
			<if test="orderNum != null">`order_num` = #{orderNum}</if>
		</set>
		where menu_id = #{menuId}
	</update>
	
	<delete id="deleteBatch">
		delete from sys_menu where menu_id in 
		<foreach item="menuId" collection="array" open="(" separator="," close=")">
			#{menuId}
		</foreach>
		;
		delete from sys_role_menu where menu_id in 
		<foreach item="menuId" collection="array" open="(" separator="," close=")">
			#{menuId}
		</foreach>
	</delete>
</mapper>